class firstApp {
    public static void main(String[] args) // public keyword used to make the main method publicely visible so that the JVM can Identify the main method to access it 
    // The static key word is used here because JVM does not have the capability to create an object.
    // public is used as an access modifier for a main method . static is used so that it can directly load in memory with creating any instance. void is used because it done not return any value and main is the entry point of program. 
    {
        System.out.println("Hello World");
    }
}
